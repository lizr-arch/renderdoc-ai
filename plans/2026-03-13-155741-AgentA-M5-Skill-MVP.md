# AgentA / M5 Skill MVP 产品化收口

时间：2026-03-13 15:57:41 | 负责人：AgentA

## Scope / Assumptions

- 主线归属：智能线。
- 依赖契约：`docs/product/development_charter.md`、`docs/product/snapshot_schema_v1.md`、`docs/product/mcp_query_contract_v1.md`。
- 写入边界仅限：
  - `tools/mcp/*`
  - `scripts/rdc_analyzer/mcp_examples/*`
  - 本计划文件
- 禁止项：不新增第二套 schema、第二套模板、第二套报告系统；不提交 `test_output` 或任何验证产物。

## Build / Test / Lint Quick Guide

- `py -3 -m pytest D:\Code\git\renderdoc-agenta-m5\tools\mcp\tests\test_snapshot_consumer.py -q`
- `py -3 D:\Code\git\renderdoc-agenta-m5\scripts\rdc_analyzer\mcp_examples\run_query.py --method get_capture_status --params "{}"`
- `py -3 D:\Code\git\renderdoc-agenta-m5\scripts\rdc_analyzer\mcp_examples\snapshot_consume.py --snapshot %TEMP%\m5_skill_contract_snapshot.json --execute --out-json %TEMP%\consumer.execute.json --out-md %TEMP%\consumer.execute.md --out-cmd %TEMP%\consumer.execute.cmds.txt`

## Task Checklist

- [x] 对齐 `snapshot_consumer.py` 的契约消费逻辑，优先使用 `snapshot.v1` 正式字段并兼容 legacy 别名。
- [x] 收敛 gap 检测与 fan-out：默认不隐式拉取纹理数据，只在显式 gap 或显式开关下补数。
- [x] 统一 dry-run / execute / blocked / partial / failed 语义，并补充 `health_probe`、`fanout`、一致的 `recovery_hint`。
- [x] 更新 `run_query.py` 与 `snapshot_consume.py`，保持单层 `mcp-query.v1` envelope，并输出固定 Markdown 简报 + 命令清单。
- [x] 扩充 `test_snapshot_consumer.py`，覆盖契约 fixture、capture status hint、bridge timeout hint、command 顺序和 fan-out。
- [x] 跑 pytest 与 live 探针；如 bridge 仍不可用，按“配置限制”记录证据与恢复路径。
- [ ] 提交到当前分支，并附 `git status --porcelain=v1 -b` 结果，确认无 `test_output` 被跟踪。

## Risks / Blockers

- 当前 live bridge 运行态仅看到 `%TEMP%\renderdoc_mcp\request.json`，未看到 `response.json`；这属于配置/运行态限制，不是协议或理论限制。
- 当前 shell 中 `git` 不在 PATH，执行提交前需要定位可用 `git.exe` 或使用显式路径。
- 当前 consumer 使用旧字段名（如 `type`、`source_code`、`render_targets`），若不做契约归一化会导致误报 gap 和多余 MCP fan-out。

## Verification / Acceptance

- [x] 契约最小快照不会被误报 `pipelines` / shader source / texture thumbnail 缺口。
- [x] `get_capture_status` 成功返回 `loaded=false` 时仍为 `ok=true`，并带一致 `recovery_hint`。
- [x] timeout 场景的 `recovery_hint` 指向 RenderDoc GUI / MCP Bridge / replay thread 检查，而不是“缩小查询范围”。
- [x] Command List 第一条固定为 `get_capture_status`。
- [x] `consumer.execute.json` 可摘要出 `enrichment.status`、`enrichment.recovery_hint`、`fanout`、`health_probe`。

## Notes

- `/do` 期间在本文件勾选完成项，并记录验证结果与阻塞证据。
- 2026-03-13 `/do` 验证结果：
  - `py -3 -m pytest D:\Code\git\renderdoc-agenta-m5\tools\mcp\tests\test_snapshot_consumer.py -q` → `10 passed in 0.05s`
  - `py -3 D:\Code\git\renderdoc-agenta-m5\scripts\rdc_analyzer\mcp_examples\run_query.py --method get_capture_status --params "{}"` → `ok=false`, `error.code=timeout`，`recovery_hint` 指向 GUI / MCP Bridge / replay thread 检查
  - `snapshot_consume.py --execute` 输出写到 `%TEMP%`：
    - `consumer.execute.json`
    - `consumer.execute.md`
    - `consumer.execute.cmds.txt`
  - `consumer.execute.json` 摘要：`status=blocked`，`health_probe.error.code=timeout`，`fanout.detail_query_count=5`，`fanout.command_count=6`
- live bridge 当前结论属于“配置/运行态限制”：
  - 证据：`%TEMP%/renderdoc_mcp` 中有 `request.json`，但无 `response.json`
  - 恢复路径：确认 RenderDoc GUI 运行、启用 MCP Bridge 扩展、检查 replay thread 是否阻塞，必要时重启扩展或 GUI 后重试 `get_capture_status`
