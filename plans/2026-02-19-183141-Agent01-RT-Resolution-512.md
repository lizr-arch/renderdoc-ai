## Scope / Assumptions
- Scope: RT 快照尺寸上调到 512；events.html 中在 RT 名称下方显示 **分辨率**（WxH）。
- In: `rdc_to_bundle_report.py` 的 RT 快照配置；`events.html` 的 RT 列表与条带展示。
- Out: ignore‑alpha 根因修复、不改导出逻辑结构。
- 假设（待验证）: `textures_data.json` 中 `width/height` 可用作 RT 分辨率来源。

## Build / Test / Lint Quick Guide (记录命令，不自动执行)
- 生成报告：`py -3 scripts/rdc_analyzer/one_click_bundle_report.py "D:\backup\EndfieldTBeta2_2025.12.18_14.36_frame42231.rdc" -o D:\backup\endfield_report`
- 视觉验证：打开 `D:\backup\endfield_report\events.html`，RT 名称下方显示 `WxH` 分辨率

## Repo / File List (含行号范围)
- `scripts/rdc_analyzer/rdc_to_bundle_report.py:39-47`（RT 快照尺寸配置）
- `scripts/rdc_analyzer/templates/events.html:2688-2710`（RT 格式/辅助函数区域）
- `scripts/rdc_analyzer/templates/events.html:2990-3024`（RT 列表渲染）
- `scripts/rdc_analyzer/templates/events.html:3030-3055`（RT 条带渲染）

## Approach (Pseudo-code / 关键实现片段)
1) **RT 快照尺寸提升到 512**
```
RT_SNAPSHOT_MAX_SIZE = 512
```

2) **events.html 增加 RT 分辨率显示**
```
function getRtSize(rt) {
  const tex = textureMap[String(rt.id || rt.resourceId)] || {};
  return (tex.width && tex.height) ? `${tex.width}x${tex.height}` : '';
}

const size = getRtSize(rt);
const sizeLabel = size ? `<div class="rt-size">${size}</div>` : '';
```
将 `sizeLabel` 放在 `rt-label` 与 `rt-format` 下方。

## Impact Analysis
- 正面：RT 内容解释更完整（格式 + 分辨率）。
- 负面：RT 标签区域增高，单个卡片高度略增。
- 风险：如果 `textures_data.json` 缺少 width/height，需优雅降级为空。

## Task Checklist (2-5 分钟粒度)
- [x] 将 `RT_SNAPSHOT_MAX_SIZE` 从 256 改为 512
- [x] events.html 增加 RT 分辨率展示（名称下方）
- [x] D:\backup\endfield_report\events.html 同步用于立即验证
- [ ] 视觉验证：RT 名称下方显示 `WxH`
- [ ] 更新本计划勾选状态 + 记录风险/决定
- [ ] Git 提交（Conventional Commits）

## Risks / Blockers
- 提升快照尺寸会增加导出耗时与报告体积（但你确认“仅展示用途”）。
- 如果浏览器 file:// 禁止 fetch，需保证 `textures_data.json` 已加载（已修复加载路径）。

## Decisions
- RT 分辨率显示位置：**RT 名称下方**（用户确认）
- 快照尺寸：**512**
- textures_data.json 字段：保留原始纹理字段 + 衍生字段 `display_name` / `simple_format` / `usedBy` / `thumbnail`

## Verification / DoD
- events.html 中 RT 名称下方显示分辨率（例如 1920x1080）
- 新报告中快照更清晰（单张视觉对比）

## Next Steps
- 等你确认 /do 后按计划执行并更新本文件勾选状态。
