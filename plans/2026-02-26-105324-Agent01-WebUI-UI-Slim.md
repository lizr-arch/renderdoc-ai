# 2026-02-26-105324-Agent01-WebUI-UI-Slim

date: 2026-02-26 10:53:24  
agent: Agent01  
stage: /plan

## Scope / Assumptions
- 目标: 精简 WebUI 交互，保留核心分析路径，移除冗余或未完成的功能点。
- 范围: index.html / textures.html / shaders.html / recommendations.html + 相关 JS + UI smoke 测试 + 交互测试矩阵文档。
- Assumption: 未检索到“精简规范”文档（search_docs 结果为空），按当前模板与交互矩阵作为事实依据。

## File List (line ranges)
- `scripts/rdc_analyzer/templates/index.html`: 420-520（统计卡片/快速导航）, 550-590（优化建议卡片/导出）, 610-680（帧缩略图与 lightbox）
- `scripts/rdc_analyzer/templates/textures.html`: 720-920（筛选/工具栏/详情操作/Lightbox/对比面板）
- `scripts/rdc_analyzer/templates/textures.html`: 1440-1625（通道/缩放/对比/Lightbox/复制 Hex 等 JS）
- `scripts/rdc_analyzer/templates/shaders.html`: 1258-1310（工具栏按钮/AI 按钮/Wrap/Search/Export）
- `scripts/rdc_analyzer/templates/shaders.html`: 1960-2055（AI 模式切换逻辑）
- `scripts/rdc_analyzer/tools/ui_headless_smoke.py`: 120-200（textures 测试中 gridView 逻辑）
- `scripts/rdc_analyzer/docs/UI_INTERACTION_TEST_MATRIX.md`: 全文（精简后同步更新）

## Proposed Slim Set (keep/remove)
- Index: 保留“统计展示 + 快速导航 + 帧缩略图预览”，移除统计卡片点击/优化建议卡片（issues 列表与导出）。
- Textures: 保留搜索/筛选/排序/列表选择/预览/属性/GUI 跳转/列表定位；移除对比模式、网格视图、通道切换、导出按钮、lightbox、颜色拾取器（未完成或冗余）。
- Shaders: 保留搜索/筛选/排序/列表选择/HLSL 展示/复制/导出/GUI 跳转/GPU 选择器/Mali 面板；移除 AI 优化模式与工具栏“换行/搜索/导出”（未绑定）。
- Recommendations: 保持不变（为问题→定位主入口）。

## Pseudocode
```
for index.html:
  remove onclick + clickable class from stats cards
  delete issues card (export links + issue list)

for textures.html:
  remove compare panel + compare button + addToCompare/clearCompareSlot
  remove grid view button + toggle handler
  remove channel buttons + applyChannelFilter stub
  remove export button (toolbar)
  remove lightbox overlay + openLightbox/closeLightbox
  remove color picker UI + copyHex entry if no longer used

for shaders.html:
  remove aiOptimizeBtn + toggleAiOptimize + ai-optimize UI
  remove toolbar wrap/search/export buttons (unbound)

update ui_headless_smoke.py:
  remove grid view test step + adjust expectations

update UI_INTERACTION_TEST_MATRIX.md:
  delete removed controls, update test steps + limitations
```

## Impact Analysis
- 交互路径更短，减少“看得到但不能用”的按钮。
- 需要同步更新 smoke 测试与交互矩阵，避免测试失真。
- 对报告功能无数据依赖改变，仅 UI/交互层面简化。

## Build/Test/Lint Quick Guide (commands only)
- UI smoke（需提供实际 bundle 报告目录）:
  - `py -3 scripts/rdc_analyzer/tools/ui_headless_smoke.py --report-dir <bundle_dir> --no-screenshots`
  - 预期输出包含: `[UI Smoke] overall_pass: True`，并生成 `ui_smoke_result.json` / `ui_smoke_result.md`。

## Task Checklist (2-5 min each)
- [x] index.html：取消统计卡片点击与“优化建议”卡片（issues 列表/导出）。
- [x] textures.html：移除对比/网格/通道/导出/Lightbox/颜色拾取器及相关 JS。
- [x] shaders.html：移除 AI 优化按钮与模式逻辑、移除未绑定工具栏按钮。
- [x] ui_headless_smoke.py：去掉对 gridViewBtn 的依赖。
- [x] UI_INTERACTION_TEST_MATRIX.md：按精简结果更新交互矩阵与测试步骤。

## Risks / Blockers
- 若某些“未完成”功能实际在运行态由外部脚本注入，则移除会影响隐藏流程（需二次确认）。
- UI smoke 依赖的功能被删后必须同步修改，否则会报错。

## Decisions
- 以“可用/可解释”为原则：未绑定或仅 UI 状态切换的功能优先移除。
- Recommendations 页保留作为主问题入口，其余页面减少重复内容。

## Verification / Acceptance (Definition of Done)
- 交互矩阵与 UI 实际页面一致，不再包含被移除按钮。
- UI smoke 可在提供的 bundle_dir 上通过（overall_pass=True）。
- 无新增 dead-end 按钮/入口。

## Next Steps
- 如需保留某些高级功能（对比/通道/AI 模式），可在后续单独补齐实现并回填矩阵。

## /do Update (2026-02-26)
- 完成 UI 精简：index/textures/shaders 移除冗余交互，recommendations 保持不变。
- 更新 UI smoke：移除 gridView 依赖，按钮重叠检测改为 HLSL vs 复制按钮。
- 交互矩阵与静态控件基线已更新。
- 验证：`py -3 scripts/rdc_analyzer/tools/ui_headless_smoke.py --no-screenshots` 失败，原因：`file:///D:/backup/endfield_report/textures.html` 超时（缺少有效 bundle_dir）。
